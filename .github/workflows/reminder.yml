name: LINEリマインダーボット

on:
  push:
    paths:
      - .github/workflows/reminder.yml
  schedule:
    - cron: '0 12 * * 2'
    - cron: '0 12 * * 6'
    - cron: '0 12 * * 0'

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - name: Echo reminder
        run: echo "リマインダーの時間です"
